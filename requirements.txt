# ── Core RAG pipeline ─────────────────────────────────────────────────────────
# Only the text splitter is used from LangChain; the heavy langchain / -openai /
# -community meta-packages pinned numpy<2 (no Python 3.13 wheel) and are unused.
langchain-text-splitters>=0.3.0
openai==1.45.0
tiktoken>=0.8.0                 # 0.7.0 has no Python 3.13 wheel (would need Rust); 0.8+ ships cp313 wheels

# ── PDF processing ────────────────────────────────────────────────────────────
pypdf2==3.0.1
pdfplumber==0.11.4

# ── Vector database ───────────────────────────────────────────────────────────
chromadb>=1.0,<2.0             # 0.5.5 pinned numpy<2 (no Python 3.13 wheel); 1.x allows numpy 2
# faiss-cpu==1.8.0             # unused (ChromaDB is the vector store); 1.8.0 has no Python 3.13 wheel

# ── API server ────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── Embeddings (free local model) ─────────────────────────────────────────────
sentence-transformers==3.1.1

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
numpy>=2.1,<2.4                # 1.26.4 has no Python 3.13 wheel; 2.1+ ships cp313 wheels
pandas==2.2.3
httpx==0.27.2

# ── Evaluation & Baselines (NLP techniques) ───────────────────────────────────
gensim==4.4.0                  # Word2Vec; 4.3.3 has no Python 3.13 wheel
scikit-learn==1.8.0            # TF-IDF, cosine similarity, metrics; 1.5.2 has no cp313 wheel
datasets>=3.0                  # Hugging Face — loads the real financial-qa-10K eval dataset

# ── Web UI (Demonstrable Output) ─────────────────────────────────────────────
streamlit>=1.35.0

# ── Notebook walkthrough ──────────────────────────────────────────────────────
matplotlib>=3.7                # charts in financial_qa_walkthrough.ipynb
jupyter>=1.0                   # to run the notebook (notebook, ipykernel, nbconvert)

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
